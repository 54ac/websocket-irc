{
	"name": "test21",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"start": "concurrently \"npm run build && node dist\\app.js\" \"npm run mongo\"",
		"build": "npm run clean && babel src -s -D -d dist --copy-files",
		"clean": "del /q dist\\*",
		"dev": "nodemon src\\app.js --exec babel-node --presets babel-preset-env",
		"mongo": "mongod --dbpath=db"
	},
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"express": "^4.17.1",
		"mongodb": "^3.3.2",
		"morgan": "^1.9.1",
		"ws": "^7.1.2"
	},
	"devDependencies": {
		"babel-preset-env": "^1.7.0",
		"nodemon": "^1.19.1"
	},
	"babel": {
		"presets": [
			[
				"env"
			]
		],
		"ignore": [
			"**\\terminal.js"
		]
	},
	"eslintConfig": {
		"globals": {
			"Terminal": true
		}
	}
}
